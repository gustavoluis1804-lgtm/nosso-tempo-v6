{
  "name": "nosso-tempo",
  "version": "4.0.0",
  "private": true,
  "description": "Nosso Tempo V4 — Live Wallpaper discreto com foto de fundo e cor por código HEX.",
  "scripts": {
    "android:init": "npx cap add android && npx cap sync android && node scripts/patch-android.mjs",
    "android:sync": "npx cap sync android && node scripts/patch-android.mjs",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3"
  }
}